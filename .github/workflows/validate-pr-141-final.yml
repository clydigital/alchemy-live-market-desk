name: Validate PR 141 Final

on:
  push:
    branches:
      - codex/market-theme-taxonomy-20260907
    paths:
      - .github/workflows/validate-pr-141-final.yml

permissions:
  contents: write

jobs:
  validate:
    if: ${{ github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: codex/market-theme-taxonomy-20260907
      - name: Full release gate
        run: |
          set -euo pipefail
          npm install --no-package-lock
          npx tsc --noEmit
          npm test
          npm run build
      - name: Remove temporary validator and generated cache
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin main
          git checkout origin/main -- tsconfig.tsbuildinfo
          rm -f .github/workflows/validate-pr-141-final.yml
          git add -A
          git commit -m "chore: remove temporary final PR 141 validator"
          git push origin HEAD:codex/market-theme-taxonomy-20260907
